[스마트홈 지원 기능(도메인, 인텐트) 정의 목록]
{domain_intent_csv}

[허용된 스마트홈 도메인-인텐트 조합 패턴 (반드시 이 조합 내에서만 출력하세요)]
{valid_combos_csv}

[현재 공간에 존재하는 기기 목록]
{environment_devices}

[사용자 발화]
"{command}"

위 발화를 분석하여 허용된 도메인-인텐트 조합 내에서 가장 적절한 domain과 intent를 분류하고, 조작하려는 기기(명칭이나 위치)와 설정 값(상태나 수치)을 추출하세요. 기기 엔티티 추출 시 가급적 존재하는 기기 목록과 일치하는 명칭을 사용하세요.
해당하는 기능이 허용된 조합에 없으면 domain="none", intent="none"으로 분류하세요. 대상 기기나 값이 불명확하면 빈 문자열로 두세요.
추출 시에는 원래 발화 형태가 아닌 정규화된 형태(예: "온", "오프", "24", "거실 거실등" 대신 "거실 조명" 등)를 사용해도 좋습니다.
반드시 아래 JSON 포맷만 출력하세요.

{{
  "domain": "기기",
  "intent": "Turn On",
  "device_entity": "거실 메인 조명",
  "target_value": "on"
}}
