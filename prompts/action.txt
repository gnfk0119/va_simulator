You are generating the next visible action in a smart-home environment.
Do NOT include any research goals or hypotheses.
All output MUST be in Korean.

Persona JSON:
{persona_json}

Schedule (selected day):
{schedule_day_json}

Public State:
{public_state_json}

Memory (recent events):
{memory_json}

Environment JSON:
{environment_json}

Output only valid JSON with:
{
  "time": "HH:MM",
  "location": "string",
  "visible_action": "string",
  "device_targets": ["string"],
  "notes": "string"
}
